# 多 Agent 架构与占卜工作流重构 · 设计文档

日期：2026-07-14
状态：初稿（待评审）

> **2026-07-14 更新**：P2（缺口 2/3/4：接场块、读人块、`set_reading_strategy`）已被
> [前置占卜师 Agent（开场幕分幕接力）](2026-07-14-opening-agent-design.md) **取代**——
> 经深入评审，开场幕改为由独立前置 Agent 台前接管（分幕接力），而非提示词块拼装（原 3.3 方案 b）。
> P1（画像卡 + 记忆 Agent）保持有效且与新设计解耦；P3 关系环不受影响。

---

## 1. 背景与动机

当前占卜核心是**单 Agent + 一组提示词**：一个 Gemini Function Calling Agent Loop 承担全部职责（意图识别、澄清、抽牌、星盘、解读、记忆读取）。四个痛点：

1. **关键行为不稳定**——"读笔记本""控制节奏""从虚到实"这些核心设计全靠提示词祈使句，模型时灵时不灵。
2. **提示词过载**——人格、流程、工具、风格混在一个大文件里互相干扰，function calling 准确率被拖累。
3. **新能力塞不下**——实时用户画像、主动问候/每日推送，在单 Agent 架构里无处安放。
4. **记忆能力设计不足**——长期记忆依赖模型主动调工具，是概率事件而非确定行为。

本文档先定义**产品要做什么**（第 2 节），再定义**技术怎么做**（第 3–5 节）。

---

## 2. 产品层重构（要做什么）

### 2.1 现有工作流拆解

把一场占卜按用户经历的顺序解剖，现状是：

| 幕 | 现在发生什么 | 设计状态 |
|---|---|---|
| ① 进场 | 通用欢迎语，邀请提问 | **空白**——对第 1 次和第 10 次来的人说同一句话 |
| ② 提问 | 预输入（硬编码）或自由输入 | 已设计，不动 |
| ③ 澄清 | 提示词一句"意图模糊时优先澄清" | **半设计**——只澄清"问题"，不读"人" |
| ④ 仪式 | AI 决定牌阵 → 按钮 → 洗牌 → 扇形选牌 | 已设计，产品亮点，不动 |
| ⑤ 解读 | 牌义 → 个性化 → 投射提问 → 叙事串联，从虚到实 | 理念完整，执行靠自觉 |
| ⑥ 反馈 | "解读是否偏离""你想到了哪件事" | 有设计，但反馈信号用完即弃 |
| ⑦ 收尾 | 不存在，对话默默衰减 | **空白** |
| ⑧ 离场后 | 12 小时后定时生成笔记 | 纯归档，用户无感知 |
| ⑨ 回访 | 与新用户零差别 | **空白** |

**结论：会话中段（②④⑤）精心设计，两端（①③⑦⑧⑨）接近裸奔。** 而两端决定关系——产品愿景是"个人专属的情感陪伴"，但当前工作流本质是**单场服务模型**：一锤子买卖做得精致，买卖之间没有关系。

### 2.2 三个视角的断层诊断

**占卜师视角——"读人先于读牌"缺失。** 真实占卜师在洗牌前已完成一半工作：判断来人是求确认还是真迷茫。README 已写出这个洞察（四类用户目标：已有答案求认同 / 辅助决策找理由 / 探索好奇 / 玄学目的，且"目标会在占卜中变化"），**但当前工作流对这个分类零使用**。同一张"恋人逆位"，对求认同者和对找决策理由者，正确策略完全不同。这个判断现在完全悬空。

**心理从业者视角——会谈没有弧线。** 咨询每一节都有结构：开场衔接 → 中段工作 → 收尾整合。用户记住一场体验的方式由峰值与结束决定（峰终定律）；峰值有设计（被命中的瞬间、抽牌仪式），结束是真空。

**C 端产品视角——留存环是断的。** 陪伴型产品的环应是：会话 → 记忆沉淀 → 回访钩子 → 回访触发 → 衔接开场 → 会话。现在五段断了三段：沉淀延迟 12 小时且不保证被使用、回访钩子从未制造、回访触发不存在。**每次回来都要重新自我介绍的产品，谈不上情感依赖。**

### 2.3 重构后的产品定义

从"一场精致的占卜"升级为"**一段被经营的占卜关系**"。

**会话内四幕：**

1. **接场**——认人但不翻旧账。老用户用熟人语气、叫昵称，**不主动提旧话题**（旧话题属于关系环，不侵入新会话）；新用户走现有欢迎引导。
2. **读人**——暗活，用户不可见。在澄清问题的同时判断用户目标与情绪浓度，产出**本场策略单**（含牌阵选型）。澄清预算**最多 1 轮**。
3. **仪式**——现有抽牌流程**原样保留**。
4. **解读**——现有方法论**原样保留**（从虚到实、投射提问、叙事串联、多次抽牌纪律）。

**离场工序（不可见）：** 会话退出/静默超时后，记忆沉淀 + 钩子提取。

> **收尾幕已评估并撤销**：它的三个价值中，回访钩子可由离场工序从对话原文提取（无需台前说出），整合已包含在现有解读收束话术中，而峰终记忆依赖"准确识别用户要走了"——该判断天然不可靠，误判提前收束等于赶客。**不做结束判断、不加结束按钮。**

**关系环（本期只出 spec，不实现）：** 离场 → 记忆沉淀（含钩子）→ 回访触发（主界面或旧会话窗口的主动接话）→ 下一场。

### 2.4 已对齐的产品决策

- **人格 ≠ 功能**。塔罗 / 占星是**功能**（同一个 Agent 都会，入口只决定起手优先级）；人格是同一占卜师的口吻风格变体（温柔型 / 直接型）。README 描述的双人格**在代码中并不存在**——两份提示词的实质差异只有工具优先级顺序。**本期人格单一，结构上留出替换位置。**
- **反馈沉淀只沉淀事实，不沉淀判词**。沉淀 MBTI、职业、感情状态、重大事件等客观画像；**绝不沉淀"准/不准"判词**。用户说"不准"是本场对话内的信号，由解读方法论当场处理（探索落差、坚持牌面立场），**不能因单次反馈就长期扭转话术**——那是占卜方法论的自毁。

---

## 3. 技术层设计（怎么做）

### 3.1 总体原则

- **纯增量**。现有 `tarot_system.md` / `astrology_system.md` **一字不动，本期不做去重**（两份文件的方法论重复是已知负债，列为后续优化）。所有新增能力以**追加块**形式由 harness 拼装在现有系统提示词之外。
- **单一台前声音**。只有占卜师 Agent 对用户说话。不引入台前导演层（每轮串行小模型判断）——双概率系统 + 每轮延迟，收益不抵成本。
- **判断力留在台前，产物落到后台**。策略判断由占卜师自己做（它有全部上下文），但产出必须是结构化对象并持久化，而非散落在对话历史里。
- **台后异步**。记忆沉淀不占用户等待时间。
- **不实现**：危机正则、情绪优先铁律、提示词去重、双人格、收尾幕。

### 3.2 架构总览

```
用户消息 ──► Harness(纯代码,毫秒级) ──► 台前占卜师 Agent(唯一出声,现有 SSE + FC Loop)
              │ 拼装系统提示词:              工具: draw_tarot_cards / get_astrology_chart /
              │  [现有系统提示词(不动)]           request_user_profile /
              │ +[画像卡(常驻)]                   read_divination_notebook(降级) /
              │ +[接场块(仅首轮)]                 set_reading_strategy(新增)
              │ +[读人块 或 策略单回显]
              │
              └─ 离场工序(退出/超时,异步) ──► 记忆 Agent(flash-lite 单次调用)
                                              产出: 画像卡更新 + 会话档案 + 钩子

关系环(本期仅 spec): 问候 Agent + 主界面/旧会话接话,消费"钩子"字段
```

**产品要求 → 保障者映射：**

| 产品要求 | 保障者 | 方式 |
|---|---|---|
| 接场·认人不翻旧账 | Harness | 首轮拼入接场块 + 代码算出的"距上次 N 天" |
| 读人·产出策略单 | 台前 Agent + 新工具 | 读人块 + `set_reading_strategy` |
| 仪式 | 现状 | 完全不动 |
| 解读·方法论 | 台前 Agent | 现有提示词，不动 |
| 被记住 | Harness | 画像卡每轮常驻注入，不再依赖工具调用 |
| 离场沉淀 + 钩子 | 记忆 Agent | 退出/超时必触发，异步 |

### 3.3 核心决策：策略单由谁产出（已被新设计取代，见文首更新说明）

三个候选：

- **(a) 台前内联，不产出结构化对象**——读人块只指导占卜师"心里判断"。零成本，但策略只存在于它说过的话里：十轮后被对话历史稀释，改判无迹可循，无法调试。这就是当前"行为不稳定"的老病根，等于没治。
- **(b) 台前 Agent 通过新工具 `set_reading_strategy` 落单** ✅ **采纳**——读人完成时（通常第 1–2 轮，与澄清同轮）提交六字段策略单，harness 存入会话状态，此后每轮压缩成一行回显注入。改判 = 覆盖调用。判断力仍 100% 留在台前唯一的声音里，没有第二个概率系统；策略跨轮持久、可见、可调试。成本：每个问题一次额外 loop 迭代（与抽牌工具同价），**不是每轮**。
- **(c) 台后每轮小模型导演层**——双概率系统 + 每轮 300–800ms 延迟 + 调试时两个嫌疑人。**否决。**

---

## 4. 缺口清单：现状 / 实现 / 影响

### 缺口 1：画像卡（含常驻注入）

**现状**：`gemini_service._build_user_context()` 只拼昵称/性别/生日/出生地；历史记忆必须靠模型主动调 `read_divination_notebook` 才拿得到——**"被记住"这个核心卖点是概率事件**。

**实现**
- 新数据结构 `data/profiles/{user_id}.json`（沿用 notebook 的 JSON 单文件模式，不引入新存储）。字段：
  - 基本信息：昵称 / 性别 / 出生信息 / 出生地（现有四项）+ 职业 / MBTI / 感情状态
  - 重大生活事件
  - 核心议题及复现次数
  - 近期重复牌面
  - 上次会话一句话摘要
  - 禁区话题
  - **全部事实型，无任何判词**
- 新增 `services/context_service.py`：读画像卡 → 渲染文本块。
- `_format_messages_for_gemini()` 中，`_build_user_context()` 调用替换为 `context_service.build_profile_card()`（现有四字段是其子集，向后兼容），**每轮无条件注入**。
- `read_divination_notebook` 保留，工具描述改为"查看某次占卜的完整原文"，不再承担"记住用户"的职责。
- 硬限长 500 tokens。

**影响**
- ✅ "被记住"从概率变确定——四个痛点中收益最大的一项。
- ✅ 省掉大部分 `read_divination_notebook` 调用 = 每次少一轮 loop 迭代（一次完整 API 往返），**延迟与 token 双双净减**。
- ⚠️ 每轮固定 +300–500 tokens 输入；与省下的 loop 相比仍净赚。
- ⚠️ 画像卡质量直接决定解读质量，写脏会污染每一轮 → 靠硬限长 + 记忆 Agent 侧采集边界约束。
- 前端零改动。

### 缺口 2：接场块

**现状**：首轮对所有人说同一句欢迎语。

**实现**
- 新提示词文件 `stage_greeting.md`，登记进 `PROMPT_REGISTRY`（管理页可在线编辑）。
- `context_service` 判定：`len(messages) == 0` 且画像卡存在 → 回头客变体（熟人语气、叫昵称、可用代码算出的"距上次 N 天"事实、**明令禁止主动翻旧话题**）；无画像卡 → 新客变体（沿用现有欢迎语调）。
- 非首轮不注入。
- 内容边界由 harness 定义，**具体文案由项目方撰写**。

**影响**
- ✅ 老用户开场不再被当陌生人——关系感最便宜的一次兑现。
- ⚠️ 仅首轮 +约 200 tokens，其余轮次为零。
- ⚠️ 风险：画像卡就在上下文里，模型可能忍不住翻旧账 → 靠块内明令压住，联调重点验证项。
- 前端零改动。

### 缺口 3：读人块 + 牌阵选型

**现状**：只有"意图模糊时优先澄清"一句；牌阵选择只有"你来决定"，**无任何选型依据**。

**实现**
- 新提示词文件 `stage_reading_person.md`，登记进 `PROMPT_REGISTRY`。内容边界（**文案由项目方撰写**）：
  - 四类用户目标的**措辞判据**（措辞泄露目标："我们还有可能吗"=求认同；"我该接 offer 还是留下"=辅助决策；"我为什么总遇到这种人"=探索好奇）
  - 情绪浓度判断
  - **澄清预算：最多 1 轮**；措辞已清晰则零澄清直接进仪式
  - **牌阵选型表**（问题类型 × 目标类型 → 牌阵与各位置含义）——当前完全空白的一块
  - 解读策略三选一：验证式 / 决策式 / 探索式
  - 判断完成即调 `set_reading_strategy`
  - **分类结论绝不外露给用户**
- `context_service` 判定：本会话策略单未设 → 注入该块；已设 → 改为注入一行策略单回显（例："本场：验证式 / 求认同 / 三张关系阵"）。

**影响**
- ✅ 补上"读人先于读牌"这个最大产品断层；牌阵从随机决定变为策略的物化。
- ⚠️ 未设策略的轮次 +约 500 tokens；设定后每轮仅 +约 50 tokens 回显。
- ⚠️ **每个问题的首轮多一次 loop 迭代**（调工具 → 返回 ok → 再出文本），首字延迟增加约一次 API 往返。这是本方案唯一的延迟成本，**每个问题只付一次，不是每轮**。
- 前端零改动。

### 缺口 4：`set_reading_strategy` 工具

**现状**：不存在，策略判断悬空。

**实现**
- `gemini_service.py` 新增 FunctionDeclaration，六字段：问题定性 / 目标类型 / 情绪浓度 / 工具路线 / 拟用牌阵 / 解读策略。
- `routers/tarot.py`、`routers/astrology.py` 的 `function_executor` 加分支：写入会话状态，返回 `{"success": true}`。**不产生 SSE 前端事件**（与抽牌不同，纯后台工具）。
- 存储：conversations 表新增 `strategy` JSON 列（已在 SQLite，加列即可）。改判 = 覆盖写。

**影响**
- ✅ 策略跨轮持久、可见、可调试——后台终于能看到"它当时凭什么这么解读"。
- ✅ 改判有确定入口：用户中途换问题 → 模型重调工具 → harness 回到执行阶段。
- ⚠️ 依赖模型愿意调该工具。不调则退化到现状（不会更糟），但需在读人块重申调用时机——**function calling 准确率是本项主要风险**。
- ⚠️ 需一次轻量 schema 变更（conversations 加列）。

### 缺口 5：记忆 Agent 产物扩展

**现状**：`notebook_system.md` 只产出 `{summary, cards_drawn}`，退出后 12 小时由定时器触发，纯归档。

**实现**
- 提示词扩为三产物：
  1. **会话档案**（现有格式不动）
  2. **画像卡增量更新**——本场新出现的事实；明确采集边界；明确禁止项（不写"准/不准"判词、不写风格推断）
  3. **钩子提取**——未完结话题一句话 + 日期（本期只落库，不消费）
- `notebook_task_scheduler`：触发时机从"退出后 12 小时"改为"**最后消息后 30 分钟未续**"；长会话每 12 轮用户消息追加一次增量更新兜底。
- 新增一次性**存量迁移脚本**：老用户历史笔记本 → 初版画像卡（否则新架构上线当天，所有老用户在系统眼里都是新人）。
- 画像卡更新采用**重写而非追加**语义，控制膨胀漂移。

**影响**
- ✅ 离场工序确定发生，画像持续生长；钩子为关系环备料。
- ✅ 沉淀提前（12h → 30min），用户当天回访即可享受更新后的画像。
- ⚠️ 每场会话一次 flash-lite 调用，异步执行，用户无感知；成本可忽略。
- ⚠️ 画像卡是累积结构，长期有膨胀与漂移风险 → 硬限长 + 重写语义 + 采集边界三重约束。

### 缺口 6：关系环（本期仅 spec，不实现）

**现状**：不存在。用户离开后产品不再出现在其生活中。

**设计（待细化）**
- **问候 Agent**：消费缺口 5 产出的钩子字段，生成一句主动接话。
- **呈现位置**（二选一或并存，待定）：主界面的占卜师主动搭话；或旧会话窗口内的延时接话。**不在新会话开场里做**——那会破坏"新会话就是新开场"的原则。
- **触发条件**：距上次会话超过 N 天、存在未消费的钩子。
- **数据依赖**：缺口 5 的钩子字段（本期已落库，届时直接消费）。

---

## 5. 改动面与分期

### 5.1 改动面清单

| 位置 | 改动 |
|---|---|
| `services/context_service.py` | **新增**——画像卡读取与渲染、阶段块判定与拼装 |
| `services/gemini_service.py` | `_format_messages_for_gemini()` 接入 context_service；新增 1 个 FunctionDeclaration。**Agent Loop 本身不动** |
| `routers/tarot.py` / `astrology.py` | `function_executor` 加 `set_reading_strategy` 分支 |
| `prompts/stage_greeting.md` | **新增**（文案由项目方撰写） |
| `prompts/stage_reading_person.md` | **新增**（文案由项目方撰写） |
| `prompts/notebook_system.md` | 扩写为三产物 |
| `prompts/tarot_system.md`、`astrology_system.md` | **一字不动** |
| `services/prompt_service.py` | `PROMPT_REGISTRY` 登记 2 个新块（白名单兼防路径穿越 + 驱动管理页列表；登记后管理页自动多出可编辑条目） |
| `services/notebook_task_scheduler.py` | 触发时机改为静默 30 分钟 |
| `data/profiles/{user_id}.json` | **新增**数据结构 |
| conversations 表 | 新增 `strategy` JSON 列 |
| `scripts/` | 新增存量画像卡迁移脚本 |
| **前端** | **零改动** |

### 5.2 综合成本影响

| 维度 | 变化 |
|---|---|
| 首字延迟 | 每个问题的读人轮 +1 次 API 往返；其余轮次不变。省掉的 `read_divination_notebook` 调用是净减 → **多数会话总体持平或更快** |
| 每轮输入 token | 现有提示词不变 + 画像卡 300–500 + 阶段块 50–500 |
| 台后成本 | 每场会话一次 flash-lite 调用（异步） |
| 前端 | 零改动 |

### 5.3 分期

- **P1 — 记忆闭环**（收益最大，先行）：缺口 1 + 缺口 5。画像卡结构、常驻注入、记忆 Agent 扩展、触发时机改造、存量迁移。此时提示词仍是老的，但"被记住"已经确定化。
- **P2 — 四幕拼装**：缺口 2 + 3 + 4。~~接场块、读人块、`set_reading_strategy`、prompt 白名单登记~~ **已由[前置占卜师 Agent 设计](2026-07-14-opening-agent-design.md)取代**。
- **P3 — 关系环**：缺口 6，**仅细化 spec，不实现**。

---

## 6. 待确认项

1. 画像卡的存储形态：独立 JSON（本文档采用）vs users 表加列——若未来要按画像字段做筛选/统计，加列更合适。
2. 长会话的画像卡兜底更新频率（暂定每 12 轮用户消息）。
3. 关系环的呈现位置（主界面 vs 旧会话窗口）——P3 细化。
