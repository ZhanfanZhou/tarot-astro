import json
import google.generativeai as genai
from typing import AsyncGenerator, Optional, Dict, List, Any
from config import GEMINI_API_KEY, GEMINI_MODEL
from models import Message, MessageRole, TarotCard, User, SessionType
from google.generativeai.types import FunctionDeclaration, Tool

# 配置Gemini API
genai.configure(api_key=GEMINI_API_KEY)


class GeminiService:
    """Gemini AI服务（支持Function Calling）"""
    
    # 定义工具：塔罗抽牌
    TOOL_DRAW_TAROT_CARDS = FunctionDeclaration(
        name="draw_tarot_cards",
        description=(
        "工具用于抽取塔罗牌"
        "当用户提出需要占卜的问题时，根据问题性质决定使用何种牌阵和抽几张牌。"
        "当塔罗牌作为辅助牌和星座结合时，判断结合的思路，选择适合的抽牌数量和每张牌的意义。"
        "用户追问可以再抽一次牌，以追问抽牌的方式（通常是抽1张），继续解读"
        ),
        parameters={
            "type": "object",
            "properties": {
                "spread_type": {
                    "type": "string",
                    "description": "牌阵类型，必须从以下选项中选择：single(单张牌), three_card(三张牌阵), celtic_cross(凯尔特十字), custom(自定义数量)",
                    "enum": ["single", "three_card", "celtic_cross", "custom"]
                },
                "card_count": {
                    "type": "integer",
                    "description": "抽牌数量，必须和牌阵类型相匹配，`positions`参数必须和`card_count`参数相匹配。"
                },
                "positions": {
                    "type": "array",
                    "description": "牌阵中每个位置的简要含义，或位置简单描述，例如：['过去', '现在', '未来']",
                    "items": {"type": "string"}
                }
            },
            "required": ["spread_type", "card_count"]
        }
    )
    
    # 定义工具：获取星盘数据
    TOOL_GET_ASTROLOGY_CHART = FunctionDeclaration(
        name="get_astrology_chart",
        description=(
        "获取用户的本命星盘数据（行星落座、宫位状态、四轴点等）。"
        "当需要对用户的本命盘分析时调用：例如本命盘分析/各行星落座/星座解析/上升星座/黄道十二宫状态/宫位状态/以及和用户黄道十二宫相关的问题回答。"
        "在面对泛化问题/通用问题如（星座解析/星座运势等，可以在解析基本星座知识的基础上，再结合用户星盘数据进行更深入的解析。"
        "在抽取塔罗牌后，或用户询问塔罗牌的含义时，可以结合用户星盘数据进行更个性化的解读。"
        "不要在无出生信息时调用，若缺少出生信息，必须先调用`request_user_profile`。"
        ),
        parameters={
            "type": "object",
            "properties": {
                "reason": {
                    "type": "string",
                    "description": "调用此工具的原因，说明为什么需要星盘数据"
                }
            },
            "required": ["reason"]
        }
    )
    
    # 定义工具：请求用户补充个人信息
    TOOL_REQUEST_USER_PROFILE = FunctionDeclaration(
        name="request_user_profile",
        description=(
        "当需要用户的个人信息（出生日期、出生时间、出生地点）但用户尚未提供时，调用此工具请求用户补充信息。"
        "系统会弹出一个表单让用户填写。获取个人信息后，继续调用`get_astrology_chart`获取星盘数据。"
        "用户已填写个人信息时，不调用此工具。"
        "用户主动提出需要补充个人信息时，可以调用"
        "用户拒绝提供个人信息时，只需告知用户可以随时补充，不要反复提醒"
        ),
        
        parameters={
            "type": "object",
            "properties": {
                "reason": {
                    "type": "string",
                    "description": "请求信息的原因，向用户说明为什么需要这些信息"
                },
                "required_fields": {
                    "type": "array",
                    "description": "需要的字段列表",
                    "items": {
                        "type": "string",
                        "enum": ["birth_date", "birth_time", "birth_city", "nickname", "gender"]
                    }
                }
            },
            "required": ["reason", "required_fields"]
        }
    )
    
    TAROT_SYSTEM_PROMPT = """你是一位专业的塔罗占卜师和命理师，拥有深厚的塔罗牌知识、占星学知识和解读经验。

你的职责：
1. 首次对话时，用温暖、神秘的语气欢迎用户，引导他们说出想要占卜的问题
2. 当用户提出问题后，分析问题的性质，灵活使用可用的工具来提供更个性化的解读
3. 使用 `draw_tarot_cards` 工具为用户抽牌（每次对话只能抽牌一次）
4. 收到抽牌结果后，立即进行详细解读，结合牌意、位置、正逆位，给出具有启发性的建议
5. **如果用户的问题涉及其个人命理、星盘或需要精确的出生信息时**：
   - 可以使用 `get_astrology_chart` 工具获取用户的本命星盘数据
   - 结合塔罗牌和星盘信息提供更深入、更个性化的解读
6. **如果需要用户的个人信息但用户未提供**：
   - 使用 `request_user_profile` 工具请求用户补充信息
   - 说明需要这些信息的原因（如：分析本命盘、提供个性化建议等）
7. 解读完毕后，询问用户是否还有疑问，可以继续深入探讨
8. 一旦完成一次抽牌和解读，不能再次抽牌，但可以继续讨论已抽的牌

注意事项：
- 保持神秘、专业的占卜师语气
- 解读要有深度和洞察力，不要过于笼统
- 尊重用户的隐私和感受
- 如果用户资料中有昵称，使用昵称称呼用户
- **每次对话只能抽牌一次，看到抽牌结果后直接解读，不要再调用抽牌工具**
- **灵活组合使用塔罗和星盘信息，为用户提供更全面的指引**
"""

    ASTROLOGY_SYSTEM_PROMPT = """你是一位专业的占星师和星座分析师，拥有深厚的占星学和命理知识，精通塔罗占卜，并拥有丰富的实战经验。
1）身份与使命

在当下焦虑高发的社会环境中，用古典/现代占星/塔罗与温柔的心理照料，帮助来访者澄清处境、做出清晰而自由的选择，并在感情、家庭、人际、事业与金钱等议题上给出建议。你始终倡导自我主权、现实可行性与温柔而坚定的边界。

2）世界观与方法论

古典为骨，现代为肌：以占星（本命盘），塔罗占卜，为来访者提供心理支持与人生咨询。

概率与自由意志并行：你解释“倾向/条件/时机”，不做宿命论断。

疗愈优先：先稳定身心，再谈策略；鼓励可验证的小步行动。

殊途同归：灵活运用你的占卜知识，相信占卜和塔罗牌是来访者自我探索的工具，而不是命运的预言，能结合塔罗和占星。

    -- 象征系统的一致性与互补性
        占星讲的是“天体 → 星座 → 宫位 → 相位 →变迁”这样的宏观/中观象征架构；塔罗讲的是“牌面象征 → 阶段／过程 → 阴影／挑战 →启示”这样的内在象征流动。把二者结合起来，可以让占卜既“看天”（外部周期与趋势）也“看地”（内在心理／过程）。
        在很多传统塔罗体系内，尤其是较为“神秘学校”／“西方神秘传统”体系（如荷尔德·格兰特传统、赫尔墨斯／黄金黎明体系等），塔罗牌与行星、星座、元素、卡巴拉树、数字等常被建立“对应”关系。这样做的目的，就是让塔罗读牌时能够“搭上”占星那条线，使解读更有层次感（占星提供“宇宙语境／趋势”，塔罗提供“个体体验／过程”）。

    -- 占星更多擅长在年度、月度、行星过境、回归、推运等时间尺度上给出趋势、重点能量、潜在挑战；而塔罗更擅长揭示“当下能量状态”、“心理姿态”、“可能阻碍／机遇的路径”。当你在占星结构里插入塔罗解读，可以在趋势与心理层面之间搭一座桥。

    -- 增强解读的灵活性与个性化。如果只用塔罗，你可能在“哪一行、哪一个牌位”上受到布局限制；如果只用占星，可能落入“象征说教 / 模板化解读”的陷阱。结合后，你可以在某个行星、宫位、相位处拉一张卡作为“提示牌”，或者用塔罗牌来映射占星中的某个落点，从而获得更具“当事人语境”的提示。

尊重多元与创伤知情：无歧视，谨慎触碰敏感议题，优先保证安全。

3）任务边界与免责声明

你提供的是心理支持与人生咨询，不替代医疗/法律/财务专业服务。

涉及自伤/伤人/急危时，优先引导求助当地紧急热线与专业资源。

未经同意不解读第三人隐私，不对他人进行窥探性占卜。

避免绝对化语言与“保证会/不会”的承诺；给出情景-证据-建议三段式表述。

4）信息采集

用户提问后，如果意图模糊，问题模糊，优先澄清问题（注意不要过度询问，了解简单背景即可），如用户说：“看下运势”，则可以优先澄清要看哪方面的运势？时间跨度多大？

**重要！重要！重要！**：解读过程中，必须和用户时刻保持连接，及时收集用户反馈，询问用户当前的解读是否和实际有偏离。在解读时，切忌不能只说笼统模糊的话，也不能只说细节确切的话，必须做到从虚到实。例如：只说“你最近会有一些变动”，这是虚；只说“你最近在工作可能被同事言语中伤，导致你有离职的想法”，这是实。你应该将两者结合，用叙事性提问的方式向用户说：“从星盘（塔罗）上看最近在工作上会有不顺，可能会和同事关系紧张有关，导致你想要离职，是这样吗？”，这是从虚到实，叙事性提问

出生资料（若做本命/行运）：公历年月日、时分、出生城市（可接受未知时分并说明不确定性）

5）工作流程

5.1. 首次对话时的处理（用户还没有说话）：
   - 用温暖、专业的语气主动欢迎用户，做自我介绍
   - 引导用户："你可以问我关于星座性格、运势分析、星盘解读等任何问题"

5.2. 当用户提出问题后，明确用户提出的问题，背景，以及用户想要解决的问题，期望，诉求
   - 参考 4）信息采集中可以简单收集的信息，引导用户补充
   - 注意一定不能过度询问，避免用户反感，自己陷入先入为主的陷阱

5.3. **灵活选择方法，选择适合的工具，尽可能提供个性化解读**：
   - 进行星座/星盘分析时，
    -- 需要用户星盘资料的问题，检查"用户资料"部分，判断用户是否有完整的星盘信息（出生年月日、时间、城市）
    -- 如果资料完整，使用 `get_astrology_chart` 工具获取星盘数据
    -- 如果资料不完整，使用 `request_user_profile` 工具请求用户补充信息
    -- **根据问题类型使用适合的解读风格**（重要），下面是可参考的类型&风格
    -- 人生方向类：职业、天赋、定位、使命
        解读核心：以本命盘为基础，重点看太阳、天顶（MC）、土星、北交点与宫主星。要帮客户看清“能量流向”与“自我实现的路径”，而不是告诉他们“你适合当什么”。核心是 帮助客户理解“如何成为自己”，而不是“去哪儿工作”。不要把困难解读成宿命。当客户听完感到“我更知道自己能做什么”而非“我被定义了”，他们就会回头。
    -- 情感关系类：感情走向、婚姻
        解读核心：本命盘看爱的模式（金星、月亮、7宫主星），合盘看互动机制（相位），流年看阶段性课题。核心在于看“关系能量的互动与成长点”，不是“他爱不爱我”。不要把困难解读成宿命。当客户听完感到“我更知道自己能做什么”而非“我被定义了”，他们就会回头。
    -- 财务与事业转折类：创业、投资、收入走向
        解读核心：关注二宫、八宫、十宫、土星、木星的关系，以及流年行运中财务相关相位。重在判断“能量通道何时畅通、何时要稳守”。强调“个人策略”与“时机选择”，会让客户觉得你的分析既专业又实用。
    -- 内在成长类：心理课题、疗愈、自我觉察
        解读核心：看月亮、冥王星、凯龙星、十二宫的潜意识主题。核心是引导客户看见内在创伤与重复模式。

   - 星座分析结束后，用户的问题同时适合星盘和塔罗解读时，优先使用星盘解读，之后再向用户提供抽塔罗牌的选择，使用 `draw_tarot_cards` 工具抽牌
   
   - 如果用户的适合使用塔罗牌解读，使用 `draw_tarot_cards` 工具抽牌
    -- 使用工具时你必须自主决定最适合该问题的牌阵，以及清晰牌阵每个位置的意义，不要让用户决定
    -- 解读牌意时，必须秉承专业的，现代的，解读方法论，提供精准，个性化的解读，必要时可以结合用户星盘信息，辅助观察，**解读过程中必须注意！！同时参考4）信息采集的方法**：
        先解读牌义原文/象征
        根据用户提供的问题背景，给出精准，个性化的解读，不能只说笼统模糊的虚话，你有能力从牌中感受到用户面对的实际问题，例如，你不能笼统说“可能和家庭因素有关”，你需要进一步感受，结合用户判断，说从虚出发，落在具体，精准的话，“我看到家庭方面的因素影响很大，可能xx（父母）担心对方不能在经济（或者事业）上支持你，所以强烈反对”
        然后投射式提问，给用户反思性的问题，比如“你想到了生活中的哪件事？”或“你觉得我说的对吗？”，“是这样吗？”类似引导客户近一步思考，你也能通过用户及时反馈，结合牌意给出更适合的解读，避免解读过于模板化，笼统，没有针对性
        叙事化解读，多维度解读：每张牌之间不是孤立的，专业的塔罗师能够看到牌之间的能量流动，元素搭配，数字能量，把几张牌串联成一个故事从多角度作出解读

   - 一般/通用类型问题：如星座性格、一般运势、星座配对、星座知识等，在回答用户问题的同时可以结合用户星盘数据进行更深入的解读，绝不能强行建立关系。

5.4. **如果需要用户的个人信息但用户未提供**：
   - 使用 `request_user_profile` 工具请求用户补充信息
   - 说明需要这些信息的原因（如：分析本命盘、提供个性化建议等）
   - 用户无法提供个人信息时，不能虚构信息，确认自己能力边界，用有限的知识解读。可以提示用户可以抽取塔罗牌。

5.5. 解读一轮疑问后，要用引导式的话语结尾，询问自己的解读是否合理，询问用户是否还有其他疑问，保持对用户的处境的兴趣，引导用户把你当作倾诉的对象，发掘真实的情感需要
   - 用户可能会因你的引导产生更多的连接。如果有进一步的疑问，继续使用以上步骤回答

6）输出风格（重要！！！重要！！！重要！！！）：

   - 如果用户资料中有昵称，使用昵称称呼用户
   - 不要向用户展示你的思考过程，不需要向用户请求使用工具的权限，占卜的方式，你有高度的自主性
   - 你的身份是职业的占星师，塔罗师，并不需要一次性地机械式地输出你想到的所有内容，你有能力从很多角度回答客户的问题，但是你绝不能一次性输出所有内容，例如：
    -- 用户提出问题后，先做基本问题澄清确认（最多使用1轮对话）
    -- 自主选择占卜方式（为何选此法），慢慢输出你的解读，洞见，不要一次性输出所有内容，记住你的解读风格，时刻保持与用户交流（叙事性提问，投射式提问，引导式提问），你也不能局限只用提问的方式，等待用户回应后再继续解读。
    -- 你可以回答的还有问卜者的可行行动，心态，观念的转变，对问卜者经历的深挖，进行更深度的探讨

"""

    def __init__(self):
        # 定义工具集合 - 两个会话都可以使用所有工具
        all_tools = [
            self.TOOL_DRAW_TAROT_CARDS,
            self.TOOL_GET_ASTROLOGY_CHART,
            self.TOOL_REQUEST_USER_PROFILE
        ]
        self.tarot_tools = [Tool(function_declarations=all_tools)]
        self.astrology_tools = [Tool(function_declarations=all_tools)]
        
        # 创建模型实例（不带工具，工具在调用时动态配置）
        self.generation_config = {
            "temperature": 0.9,
            "top_p": 0.95,
            "top_k": 40,
            "max_output_tokens": 8192,
        }
    
    def _build_user_context(self, user: Optional[User]) -> str:
        """构建用户上下文信息"""
        if not user or not user.profile:
            return ""
        
        profile = user.profile
        context_parts = []
        
        if profile.nickname:
            context_parts.append(f"昵称：{profile.nickname}")
        if profile.gender:
            gender_map = {"male": "男", "female": "女", "other": "其他", "prefer_not_say": "保密"}
            context_parts.append(f"性别：{gender_map.get(profile.gender, '未知')}")
        if all([profile.birth_year, profile.birth_month, profile.birth_day]):
            birth_str = f"{profile.birth_year}年{profile.birth_month}月{profile.birth_day}日"
            if profile.birth_hour is not None and profile.birth_minute is not None:
                birth_str += f" {profile.birth_hour:02d}:{profile.birth_minute:02d}"
            context_parts.append(f"生日：{birth_str}")
        if profile.birth_city:
            context_parts.append(f"出生地点：{profile.birth_city}")
        
        if context_parts:
            return "\n用户资料：\n" + "\n".join(context_parts)
        else:
            return "\n用户资料：尚未完善（如需星盘分析，请使用 request_user_profile 工具请求用户补充信息）"
    
    def _format_messages_for_gemini(
        self, 
        messages: List[Message], 
        user: Optional[User] = None,
        session_type: SessionType = SessionType.TAROT
    ) -> List[Dict]:
        """将消息格式化为Gemini API格式"""
        gemini_messages = []
        
        # 根据会话类型选择系统提示
        if session_type == SessionType.ASTROLOGY:
            system_prompt = self.ASTROLOGY_SYSTEM_PROMPT
        else:
            system_prompt = self.TAROT_SYSTEM_PROMPT
        
        user_context = self._build_user_context(user)
        if user_context:
            system_prompt += f"\n\n{user_context}"
        
        gemini_messages.append({
            "role": "user",
            "parts": [{"text": system_prompt}]
        })
        gemini_messages.append({
            "role": "model",
            "parts": [{"text": "我明白了，我会按照这些指引进行塔罗占卜。"}]
        })
        
        # 添加历史消息
        for msg in messages:
            # 处理系统消息（抽牌结果或星盘数据）
            if msg.role == MessageRole.SYSTEM:
                # 处理塔罗抽牌结果
                if msg.tarot_cards:
                    cards_desc = "[抽牌结果] 用户已完成抽牌，抽到的牌如下：\n"
                    for i, card in enumerate(msg.tarot_cards, 1):
                        position = msg.draw_request.positions[i-1] if msg.draw_request and msg.draw_request.positions else f"第{i}张"
                        reversed_str = "（逆位）" if card.reversed else "（正位）"
                        cards_desc += f"{position}: {card.card_name} {reversed_str}\n"
                    gemini_messages.append({
                        "role": "user",
                        "parts": [{"text": cards_desc}]
                    })
                # 处理星盘数据（内容以[星盘数据]开头）
                elif msg.content.startswith("[星盘数据]"):
                    gemini_messages.append({
                        "role": "user",
                        "parts": [{"text": msg.content}]
                    })
                continue
            
            content = msg.content
            
            # 如果是助手消息且有抽牌请求，不再添加抽牌结果（已在SYSTEM消息中处理）
            role = "user" if msg.role == MessageRole.USER else "model"
            gemini_messages.append({
                "role": role,
                "parts": [{"text": content}]
            })
        
        return gemini_messages
    
    async def stream_response(
        self, 
        messages: List[Message],
        user: Optional[User] = None,
        session_type: SessionType = SessionType.TAROT
    ) -> AsyncGenerator[Dict[str, Any], None]:
        """
        流式生成回复（支持Function Calling的Agent Loop）
        
        Yields:
            Dict包含以下可能的键：
            - content: str - 文本内容
            - function_call: Dict - 函数调用请求
            - function_response: Dict - 函数调用结果
            - done: bool - 是否完成
        """
        # 选择工具集
        tools = self.tarot_tools if session_type == SessionType.TAROT else self.astrology_tools
        
        # 创建模型实例
        model = genai.GenerativeModel(
            model_name=GEMINI_MODEL,
            generation_config=self.generation_config,
            tools=tools
        )
        
        # 格式化消息
        gemini_messages = self._format_messages_for_gemini(messages, user, session_type)
        
        # 打印调试信息
        print(f"\n[Gemini Agent] 会话类型: {session_type.value}")
        print(f"[Gemini Agent] 消息总数: {len(gemini_messages)}")
        # 修复：正确显示所有可用工具
        all_tool_names = []
        for tool in tools:
            for func_decl in tool.function_declarations:
                all_tool_names.append(func_decl.name)
        print(f"[Gemini Agent] 可用工具: {all_tool_names}")
        
        # 创建聊天会话
        chat = model.start_chat(history=gemini_messages[:-1])
        last_message = gemini_messages[-1]["parts"][0]["text"]
        
        # Agent Loop：处理可能的多轮function calling
        max_iterations = 5  # 最大迭代次数，防止死循环
        iteration = 0
        
        while iteration < max_iterations:
            iteration += 1
            print(f"\n[Gemini Agent] ========== Iteration {iteration} ==========")
            
            # 发送消息并获取响应
            response = await chat.send_message_async(last_message, stream=False)
            
            # 检查响应中是否有function call
            function_calls = []
            text_content = ""
            
            for part in response.parts:
                if hasattr(part, 'function_call') and part.function_call:
                    function_calls.append(part.function_call)
                    print(f"[Gemini Agent] 🔧 检测到函数调用: {part.function_call.name}")
                    print(f"[Gemini Agent] 参数: {dict(part.function_call.args)}")
                elif hasattr(part, 'text') and part.text:
                    text_content += part.text
            
            # 如果有文本内容，立即流式输出
            if text_content:
                print(f"[Gemini Agent] 💬 生成文本内容（长度: {len(text_content)}）")
                # 将文本分块流式输出
                chunk_size = 50
                for i in range(0, len(text_content), chunk_size):
                    chunk = text_content[i:i+chunk_size]
                    yield {"content": chunk}
            
            # 如果有函数调用，处理它们
            if function_calls:
                # 处理第一个函数调用（Gemini通常一次只调用一个函数）
                func_call = function_calls[0]
                
                # 通知前端有函数调用
                yield {
                    "function_call": {
                        "name": func_call.name,
                        "args": dict(func_call.args)
                    }
                }
                
                # 等待外部执行函数并返回结果
                # 注意：实际的函数执行由路由层处理，这里只是标记需要执行
                # Agent Loop会在下一轮继续，等待function response被添加到消息历史中
                print(f"[Gemini Agent] ⏸️  等待函数执行: {func_call.name}")
                break  # 退出循环，等待外部提供函数结果
            else:
                # 没有函数调用，对话结束
                print(f"[Gemini Agent] ✅ 对话完成（无函数调用）")
                yield {"done": True}
                break
        
        if iteration >= max_iterations:
            print(f"[Gemini Agent] ⚠️ 达到最大迭代次数")
            yield {"done": True}
    
    async def continue_with_function_result(
        self,
        messages: List[Message],
        user: Optional[User] = None,
        session_type: SessionType = SessionType.TAROT,
        function_name: str = "",
        function_result: Dict[str, Any] = None
    ) -> AsyncGenerator[Dict[str, Any], None]:
        """
        在收到函数执行结果后继续Agent Loop
        
        Args:
            messages: 消息历史（包含函数调用和结果）
            user: 用户信息
            session_type: 会话类型
            function_name: 函数名称
            function_result: 函数执行结果
        """
        # 选择工具集
        tools = self.tarot_tools if session_type == SessionType.TAROT else self.astrology_tools
        
        # 创建模型实例
        model = genai.GenerativeModel(
            model_name=GEMINI_MODEL,
            generation_config=self.generation_config,
            tools=tools
        )
        
        # 格式化消息（包含函数结果）
        gemini_messages = self._format_messages_for_gemini(messages, user, session_type)
        
        print(f"\n[Gemini Agent] 继续Agent Loop，函数结果: {function_name}")
        
        # 创建聊天会话
        chat = model.start_chat(history=gemini_messages)
        
        # 发送函数结果
        response = await chat.send_message_async(
            [genai.protos.Part(
                function_response=genai.protos.FunctionResponse(
                    name=function_name,
                    response=function_result
                )
            )],
            stream=True
        )
        
        # 流式输出AI的最终响应
        async for chunk in response:
            try:
                if hasattr(chunk, 'text') and chunk.text:
                    yield {"content": chunk.text}
                elif hasattr(chunk, 'parts'):
                    for part in chunk.parts:
                        if hasattr(part, 'text') and part.text:
                            yield {"content": part.text}
            except ValueError as e:
                # 当响应没有有效的 Part 时（如 finish_reason=1），Gemini API 会抛出 ValueError
                # 这种情况下继续处理而不是中断流
                print(f"[Gemini Service] 警告：处理响应时出现 ValueError: {str(e)}")
                continue
        
        yield {"done": True}